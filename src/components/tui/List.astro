---
export interface Props {
  as?: 'ul' | 'ol';
  ariaLabel?: string;
  class?: string;
}

const { as: Tag = 'ul', ariaLabel, class: className = '' } = Astro.props;
---

<Tag
  role="list"
  aria-label={ariaLabel}
  class={`font-tui text-sm space-y-[2px] ${className}`}
>
  <slot />
</Tag>
